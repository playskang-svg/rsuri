# 이 앱은 정적 export 빌드 타임에만 Supabase를 읽는다 — anon 키만 필요, service_role은 여기 없음.
# 실제 값은 .env.local(커밋 금지)에 넣을 것. 값 자체는 루트 .env.example과 동일한 프로젝트를 가리킨다.
NEXT_PUBLIC_SUPABASE_URL="https://xparlhzbactezsvuteto.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="sb_publishable_JHkDFy4-3xy_POVdix6-jg_tJv1vIV_"
